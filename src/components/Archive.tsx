import { Container, Row } from "react-bootstrap";
import { ArchiveFill } from "react-bootstrap-icons";
import { File } from "../interfaces/file";

const Archive = () => {
  return (
    <Container>
      <Row>Plik jest spakowany, brak kontentu.</Row>
    </Container>
  );
};

export default Archive;
